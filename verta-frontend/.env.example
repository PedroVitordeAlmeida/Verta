# URL base da API do backend Verta (Ktor). Em desenvolvimento local,
# normalmente http://localhost:8080. Dentro do docker-compose, use o nome
# do servico do backend, ex: http://backend:8080
VITE_API_URL=http://localhost:8080
